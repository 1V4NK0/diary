import styles from './DisplayEntry.module.css'
import { useParams } from "react-router-dom";

function DisplayEntry() {
  
  return <div className={styles.main}>DISPLAY A Entry</div>;
}

export default DisplayEntry;
